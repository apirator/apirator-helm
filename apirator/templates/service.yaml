apiVersion: v1
kind: Service
metadata:
  name: {{ include "apirator.fullname" . }}
  labels:
    {{- include "apirator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apirator.selectorLabels" . | nindent 4 }}
